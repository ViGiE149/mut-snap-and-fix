import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.mutInovationLab.starter',
  appName: 'Snap and Fix',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
